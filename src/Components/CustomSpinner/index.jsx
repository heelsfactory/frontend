import React from 'react';
import './CustomSpinner.css'; // Import the CSS file for styling

const CustomSpinner = () => {
  return (
    <div className="custom-spinner">
      <div className="spinner"></div>
    </div>
  );
};

export default CustomSpinner;
